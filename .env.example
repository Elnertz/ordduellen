# Only needed when packaging the native app (Capacitor). For the web version,
# leave this unset — the client talks to the same origin it is served from.

# Absolute URL to your deployed backend, e.g. https://ordduellen.onrender.com
VITE_API_BASE=
